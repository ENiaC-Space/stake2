# Clone repository
git clone https://github.com/eniac-space/stake2.git
cd stake1

# Tạo file .nojekyll
touch .nojekyll

# Tạo file index.html đầy đủ (dùng code bên dưới)

# Commit và push
git add .
git commit -m "Initial commit with GitHub Pages fix"
git push origin main
